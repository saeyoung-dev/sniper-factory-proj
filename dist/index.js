"use strict";
{
    /**
     * Union Type | (or)
     * 피연산자 중에서 truthy한 값을 만족할 경우
     */
    let strAndNum = 10;
    strAndNum;
    let user = {
        name: 'ellie',
        money: 0,
    };
    const arr = [1, 'effy'];
    /**
     * Intersection Type && (and)
     * 모든 피연산자가 truthy 조건을 만족한 경우
     */
    const intership = {
        isStudent: true,
        isWorking: true,
    };
}
{
    const user = {
        name: 'effy',
        age: 20,
    };
}
